# Serving dependencies for the Streamlit app only.
# Streamlit Community Cloud ignores runtime.txt and builds with its current
# default Python (3.13/3.14), so every range below must have binary wheels
# for Python 3.11 through 3.14. Training-only dependencies live in
# requirements-dev.txt.
streamlit>=1.45,<1.60
requests>=2.31,<3
pandas>=2.2,<3
numpy>=1.26,<3
scikit-learn>=1.5,<2
plotly>=6.1.1,<7
pyarrow>=14,<30
openpyxl>=3.1,<4
scipy>=1.11,<2
pycountry-convert==0.7.2
