*.csv filter=lfs diff=lfs merge=lfs -text
cache/*.parquet filter=lfs diff=lfs merge=lfs -text
cache/*.pkl filter=lfs diff=lfs merge=lfs -text

# Small bootstrap metadata required during CI collection must remain available
# when the workflow intentionally skips large Git LFS source artifacts.
data/reference/systemic_banking_crises_1970_2025.csv !filter !diff !merge text
