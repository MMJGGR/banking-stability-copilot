# PRD amendment v0.4 — uncapped representation sensitivity

Date: 2026-09-22. Continues the owner's authorized breakaway-branch execution. Applies with v0.1–v0.3. No production change, merge, promotion or crisis-classifier retraining is authorized.

## Purpose and fixed scope

The already executed v0.3 research fits 9,644 varying reference inputs from the complete 15,295-representation contract; it does not restrict predictors to the three supervised outcomes. Keep that primary result unchanged. The next target-independent diagnostic asks how much the geometry depends on repeated measurement profiles or uneven source representation. It is not a new forecasting contest, a feature-selection rule, an economic weighting decision or evidence of model superiority.

Use the same immutable September 16 M2 artifact 10463106988 (ZIP SHA256 f670f2e7caff532201c1a198130d0f8b34cb04784cb643dae2c5466af9359c9b), verified by its content ledger. Reuse the exact v0.3 unit-admission and causal within-entity amount normalization. Reference origin is 2023, with observations through 2022 under the declared one-year lag. Compare the previous year's inputs only under the frozen reference. These are retrospective reference descriptions, not present-day 2026 forecasts or historical predictions made in 2022. No target file needs to be loaded or outcome evaluated.

## Predeclared sensitivities

1. Baseline: every varying transformed input has weight one, exactly reproducing the existing descriptive PCA spectrum.
2. Exact-profile-balanced: each predictor remains present, but a group of k EXACTLY identical transformed reference profiles AND observation masks has per-column squared weight 1/k. This is mathematically equivalent to one copy of the profile for reference covariance while retaining all provenance. It must reproduce the existing exact-profile-collapse spectrum within numerical tolerance. Weights are fitted on the reference only and remain frozen on other snapshots; subsequent profile divergence is reported, not regrouped using future observations.
3. Source-energy-balanced: all inputs remain present. Scale each source block by the inverse square root of its total reference transformed energy, so each nonempty source contributes the same pre-PCA energy. This is an explicitly chosen diagnostic sensitivity, not a claim that five sources deserve equal economic importance and not a replacement of the data-driven baseline. It identifies dependence on the measurement mix.

All weights must be positive and finite. Missing/constant inputs remain in the admission ledger with their existing reasons. No new top-k rule, economic allowlist, outcome screen, hard-coded component count, positivity constraint or development anchor is introduced. The 90% descriptive component rule and 80%/95% diagnostics are inherited from v0.3.

## Required outputs and tests

Export for all three specifications: complete spectrum, data-selected component counts, ALL feature weights, feature-level common-variance contributions, source energy/contribution summaries, observed-share and common/residual distinctness diagnostics. Common distance and observed residual error are descriptive, never risk tiers or calibrated uncertainty. Do not rank political actors or institutions. Compare geometry through correlation diagnostics without interpreting them as predictive accuracy.

Tests must cover identity preservation; positive weights; equal group contribution; profile duplication invariance of reference covariance; row/column permutation; zero/all-missing rejection at numeric-kernel boundary; source/mask alignment; exact-profile agreement only when both transformed values and masks agree; unchanged observed inputs; complete spectrum accounting; and unsupported rows. Empty or mismatched group metadata fails closed. Preserve the original v0.3 outputs, model comparisons and negative predictive results.

## App freshness boundary

The committed serving manifest remains the September 15 snapshot, not the September 16 research retrieval. The latter includes the changed MFS payload. Artifact timestamp, maximum observation date and per-feature measurement date are distinct. An accessible login page does not verify the artifact served inside an authenticated session. The current app's broad data explorer and finite active score-feature list are distinct from the research feature universe. Record those boundaries in the execution handoff; do not claim every current app feature incorporates every newer raw observation merely because the source version is unchanged.

## Acceptance

The new experiment is complete only when tests and the frozen-input execution pass, checksums are recorded, and production paths/classifier are verified unchanged. A successful descriptive sensitivity is not a promoted model or a reason to tune against the already examined forward results. The next predictive experiments still require their own declared plan and clean separation from final confirmation.
